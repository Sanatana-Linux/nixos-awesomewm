return {
    clickKey = require("clickKey"),
    colorizeText = require("colorizeText"),
    cropSurface = require("cropSurface"),
    
    rrect = require("rrect"),
    addHover = require("addHover"),
    placeWidget = require("placeWidget"),
    prect = require("prect"),
}
