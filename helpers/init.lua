-- helpers/init.lua

local helpers = {

add_hover = require("helpers.add_hover"),
add_tables = require("helpers.add_tables"),
color_darken = require("helpers.color_darken"),
color_lighten = require("helpers.color_lighten"),
colorize_text = require("helpers.colorize_text"),
crop_surface = require("helpers.crop_surface"),
file_exists = require("helpers.file_exists"),
find_last = require("helpers.find_last"),
gc = require("helpers.gc"),
generate_id = require("helpers.generate_id"),
get_widget_geometry = require("helpers.get_widget_geometry"),
has_key = require("helpers.has_key"),
hover_cursor = require("helpers.hover_cursor"),
index_of = require("helpers.index_of"),
in_table = require("helpers.in_table"),
mkbtn = require("helpers.mkbtn"),
place_widget = require("helpers.place_widget"),
prect = require("helpers.prect"),
random_color = require("helpers.random_color"),
read_file = require("helpers.read_file"),
read_json = require("helpers.read_json"),
rrect = require("helpers.rrect"),
split = require("helpers.split"),
trim = require("helpers.trim"),
write_json = require("helpers.write_json"),

}
return helpers
